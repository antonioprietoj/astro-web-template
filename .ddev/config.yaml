name: astro-web-template
type: nodejs
nodejs_version: "20"
docroot: dist

web_extra_exposed_ports:
  - name: astro-dev
    container_port: 4321
    http_port: 4321
    https_port: 4322

hooks:
  post-start:
    - exec: "if [ ! -d node_modules ]; then npm install; fi"
